{
  "name": "authwall-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'frontend placeholder build' && mkdir -p dist && cp -r src/* dist/"
  },
  "dependencies": {
    "axios": "^1.8.0",
    "vue": "^3.5.13",
    "element-plus": "^2.9.5",
    "vue-router": "^4.4.5"
  }
}
